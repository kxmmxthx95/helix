-- ชุดข้อสอบ removed — grill decision, 2026-08-21. Feature dropped before any
-- real bank had sets in use; can_write_exam_subject (0047) stays, still used
-- by exam_questions.

drop table exam_question_sets;
drop table exam_sets;
